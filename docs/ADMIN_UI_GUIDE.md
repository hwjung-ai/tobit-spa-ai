# Admin UI 운영 가이드

**작성 일시**: 2026-01-18
**상태**: 최신 (통합 문서)
**대상**: 시스템 관리자, 운영팀

이 문서는 관리자용 3개 UI 화면의 기능, 구성, 운영 절차를 통합으로 설명합니다.

---

## 📋 개요

### 3개 관리 화면
1. **Assets Admin** - 자산(Prompt, Mapping, Policy) 관리
2. **Observability Dashboard** - 시스템 성능 및 KPI 모니터링
3. **Settings** - 시스템 설정 (런타임 플래그, 환경변수)

### 접근 권한
- **필요**: 관리자 역할 (admin role)
- **경로**: `/admin` → 탭 네비게이션으로 이동

---

## 1. Assets Admin 화면

### 1.1 화면 경로
- **URL**: `/admin/assets`
- **네비게이션**: NavTabs에 "Admin" 탭 추가 (adminOnly: true)

### 1.2 화면 구성

```
┌─────────────────────────────────────────────────────────────┐
│ [Assets Admin]                                              │
├─────────────────────────────────────────────────────────────┤
│ Filter: [All Types ▼] [All Status ▼]  [+ New Asset]        │
├─────────────────────────────────────────────────────────────┤
│ Asset List (Table)                                          │
│ ┌────────────────────────────────────────────────────────┐  │
│ │ Name │ Type │ Status │ Version │ Updated │ Actions    │  │
│ ├────────────────────────────────────────────────────────┤  │
│ │ ...                                                    │  │
│ └────────────────────────────────────────────────────────┘  │
│                                                             │
│ Pagination: [< ] 1 [>] (10 per page)                       │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ [Asset Detail: {name}]                        [← Back]      │
├─────────────────────────────────────────────────────────────┤
│ Basic Info                                                  │
│ • Name: [input field]                                      │
│ • Type: [badge: prompt/mapping/policy]                     │
│ • Status: [badge: draft/published]                         │
│ • Version: {number}                                        │
│                                                             │
│ Content (Type-specific)                                    │
│ • Prompt: [template textarea] [input_schema JSON]          │
│ • Mapping: [content JSON editor]                           │
│ • Policy: [limits JSON editor]                             │
│                                                             │
│ Validation Errors (if any)                                 │
│ ⚠️ [Error messages in red alert box]                       │
│                                                             │
│ Actions                                                    │
│ [Save Draft] [Publish] [Rollback] [Delete]                │
└─────────────────────────────────────────────────────────────┘
```

### 1.3 테이블 컬럼

| 컬럼 | 타입 | 설명 |
|------|------|------|
| Name | string | 자산 이름 (클릭 시 상세 페이지) |
| Type | badge | `prompt` / `mapping` / `policy` |
| Status | badge | `draft` (회색) / `published` (녹색) |
| Version | number | 현재 버전 번호 |
| Updated | datetime | updated_at (상대 시간: "2시간 전") |
| Actions | buttons | View / Edit / Delete (draft만) |

### 1.4 주요 기능

#### 목록 화면 액션

**[+ New Asset]**
- 새 자산 생성 모달 표시
- Type 선택: Prompt / Mapping / Policy
- Name, Description 입력
- 생성 시 draft 상태로 초기화

**필터**
- **Type**: All / Prompt / Mapping / Policy
- **Status**: All / Draft / Published
- 조합 필터링 가능 (예: Draft Prompts만)

#### 상세 화면 액션

**[Save Draft]** (draft 상태일 때만)
- 현재 내용을 draft로 저장
- 버전 번호 유지
- 응답: `{success: true, asset_id, version}`

**[Publish]** (draft 상태일 때만)
- Draft → Published로 변경
- 버전 번호 +1 (예: 1 → 2)
- 이 버전이 실제 운영에 적용
- 감사 로그 기록: 누가, 언제, 뭘 발행했는가

**[Rollback]** (published 상태일 때만)
- 이전 published 버전으로 되돌리기
- 버전 선택 UI 표시
- 확인 모달: "정말 v{N}으로 되돌리시겠습니까?"
- 감사 로그 기록: rollback 사유 입력 필드

**[Delete]** (draft만 삭제 가능)
- 확인 모달 표시
- 영구 삭제 (soft delete 불가)
- Published 자산은 삭제 불가 (관리자도)

### 1.5 자산 타입별 상세

#### Prompt Asset
```json
{
  "name": "Customer Support Classifier",
  "type": "prompt",
  "status": "published",
  "version": 2,
  "content": {
    "template": "Classify the customer message...",
    "input_schema": {
      "type": "object",
      "properties": {
        "message": {"type": "string"},
        "context": {"type": "string"}
      }
    }
  }
}
```

**편집 필드**:
- Template (textarea): 프롬프트 내용
- Input Schema (JSON): 입력 파라미터 스키마
- 유효성 검사: JSON 구문 검증

#### Mapping Asset
```json
{
  "name": "API Response Transformer",
  "type": "mapping",
  "status": "draft",
  "content": {
    "input_format": "json",
    "transformations": [
      {"from": "data.user.id", "to": "userId"},
      {"from": "data.user.name", "to": "userName"}
    ]
  }
}
```

**편집 필드**:
- Content (JSON editor): 매핑 규칙
- 라이브 플리뷰: 입력 샘플 → 출력 샘플

#### Policy Asset
```json
{
  "name": "Rate Limit Policy",
  "type": "policy",
  "status": "published",
  "content": {
    "max_requests_per_minute": 100,
    "max_tokens_per_request": 5000,
    "allowed_roles": ["admin", "user"]
  }
}
```

**편집 필드**:
- Limits (JSON): 정책 파라미터
- 유효성 검사: 필드명 및 타입 검증

### 1.6 에러 처리

**유효성 검사 실패**
```
⚠️ Validation Error
- JSON syntax error at line 5: Unexpected token }
- Template contains undefined variables: {userId}, {timestamp}
```

**저장 실패**
```
❌ Save Error
Unable to publish: Dependent policy not found (id: policy_123)
```

**삭제 불가**
```
❌ Cannot Delete
This asset is published and referenced by 2 other assets.
Rollback to draft first, then delete.
```

---

## 2. Observability Dashboard

### 2.1 화면 경로
- **URL**: `/admin/observability`
- **목표**: 시스템 성능, 사용량, 이상징후 모니터링

### 2.2 화면 구성

```
┌─────────────────────────────────────────────────────────────┐
│ [Observability Dashboard]                                   │
├─────────────────────────────────────────────────────────────┤
│ Time Range: [Last 24h ▼] [Refresh ⟲]                       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ ROW 1: Key Metrics (4 cards)                               │
│ ┌──────────────┬──────────────┬──────────────┬────────────┐ │
│ │ API Calls    │ Avg Response │ Error Rate   │ Tokens     │ │
│ │ 15,234       │ 87ms         │ 0.2%         │ 2.3M/10M   │ │
│ │ ↑ 12% vs 24h │ ↓ 5% vs 24h  │ ↓ 0.1% vs 24h│ ↑ 8%       │ │
│ └──────────────┴──────────────┴──────────────┴────────────┘ │
│                                                             │
│ ROW 2: Trend Charts (2x 50%)                               │
│ ┌─────────────────────────┬───────────────────────────────┐ │
│ │ API Call Trend          │ Response Time Distribution    │ │
│ │ [Line Chart: 24h]       │ [Histogram: latency buckets]  │ │
│ └─────────────────────────┴───────────────────────────────┘ │
│                                                             │
│ ROW 3: Errors & Alerts (2x 50%)                            │
│ ┌─────────────────────────┬───────────────────────────────┐ │
│ │ Error Distribution       │ Top Slow Queries             │ │
│ │ [Pie Chart: types]      │ [Table: query, time, count]  │ │
│ └─────────────────────────┴───────────────────────────────┘ │
│                                                             │
│ ROW 4: System Health (Full width)                          │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ Database: ● Connected (14 active connections)          │ │
│ │ Cache: ● Healthy (85% hit rate)                        │ │
│ │ Queue: ● 234 pending jobs (avg 2.3s processing)       │ │
│ │ API Servers: ● 3/3 healthy (load avg: 45%, 42%, 48%)  │ │
│ └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

### 2.3 주요 메트릭

| 메트릭 | 수집 출처 | 갱신 주기 | 표시 형식 |
|--------|---------|---------|---------|
| API Calls | `TbApiCallLog` | 1분 | 누적 + 변화율 (24h 비교) |
| Avg Response Time | `TbApiCallLog` | 1분 | 평균ms + 변화율 |
| Error Rate | `TbApiCallLog` | 1분 | % + 변화율 |
| Token Usage | `TbUsageLog` | 5분 | 현재/제한 + 사용률 |
| Query Latency | `TbQueryLog` | 1분 | p50/p95/p99 |
| Cache Hit Rate | Redis | 1분 | % + miss 카운트 |

### 2.4 시간 범위

- Last 1h / Last 24h / Last 7d / Last 30d / Custom
- 자동 갱신: 30초마다 데이터 새로고침
- 수동 갱신: [Refresh ⟲] 버튼

### 2.5 알림 규칙 (선택)

| 조건 | 심각도 | 알림 |
|------|--------|------|
| Error Rate > 1% | 🔴 Critical | Slack, Email |
| Response Time p95 > 500ms | 🟠 Warning | Slack |
| Token Usage > 80% | 🟠 Warning | Dashboard badge |
| Database connections > 20 | 🔴 Critical | Slack, Email |

---

## 3. Settings (시스템 설정)

### 3.1 화면 경로
- **URL**: `/admin/settings`
- **목표**: 런타임 플래그, 환경변수 관리

### 3.2 화면 구성

```
┌─────────────────────────────────────────────────────────────┐
│ [System Settings]                                           │
├─────────────────────────────────────────────────────────────┤
│ Search: [Find settings...]                                  │
├─────────────────────────────────────────────────────────────┤
│ Settings Groups                                             │
│ ┌────────────────────────────────────────────────────────┐  │
│ │ ✓ OPS AI                                               │  │
│ │   • ops_mode (current: "real")                         │  │
│ │   • ops_enable_langgraph (current: true)               │  │
│ │   • ops_recursion_depth_max (current: 5)               │  │
│ │                                                        │  │
│ │ ✓ API                                                  │  │
│ │   • api_request_timeout_seconds (current: 30)          │  │
│ │   • api_rate_limit_per_minute (current: 100)           │  │
│ │                                                        │  │
│ │ ✓ Database                                             │  │
│ │   • db_connection_pool_size (current: 20)              │  │
│ │   • db_query_timeout_seconds (current: 60)             │  │
│ └────────────────────────────────────────────────────────┘  │
│                                                             │
│ Setting Detail                                              │
│ Name: ops_mode                                              │
│ Current Value: real  [Copy]                                 │
│ Edit: [radio: mock / real]                                 │
│ Restart Required: Yes ⚠️                                    │
│ Last Modified: 2026-01-18 14:23:00 by admin                │
│ [Save Changes] [Revert]                                    │
└─────────────────────────────────────────────────────────────┘
```

### 3.3 설정 항목

#### OPS AI Group
- `ops_mode`: mock / real (기본: real)
- `ops_enable_langgraph`: true / false (기본: false)
- `ops_recursion_depth_max`: 1-20 (기본: 5)
- `ops_cache_ttl_seconds`: 0-3600 (기본: 300)

#### API Group
- `api_request_timeout_seconds`: 10-300 (기본: 30)
- `api_rate_limit_per_minute`: 1-10000 (기본: 100)
- `api_response_compression`: true / false (기본: true)

#### Database Group
- `db_connection_pool_size`: 5-100 (기본: 20)
- `db_query_timeout_seconds`: 10-600 (기본: 60)
- `db_enable_query_logging`: true / false (기본: true)

#### Logging Group
- `log_level`: DEBUG / INFO / WARNING / ERROR (기본: INFO)
- `log_retention_days`: 1-365 (기본: 30)
- `log_json_format`: true / false (기본: true)

### 3.4 주요 기능

**설정 변경**
- 입력: 유형별 컨트롤 (텍스트, 라디오, 슬라이더)
- 유효성 검사: min/max, 허용값 검증
- 변경 이력: 누가, 언제, 뭘 변경했는가 기록

**재시작 요구 여부**
- 즉시 적용되는 설정: ⚡ (별도 표시 없음)
- 재시작 필요: ⚠️ 재시작 버튼 활성화

**감시 알림**
- 주요 설정 변경 시 Slack, Email 알림
- 변경 승인 정책 (선택)

---

## 4. 운영 절차

### 4.1 자산 발행 절차

```
1. 개발팀이 Draft 작성 (또는 외부 API로 생성)
   ↓
2. 관리자 검토 (Assets Admin → 상세 페이지)
   - 내용 검증: 템플릿, 스키마 확인
   - 테스트 (선택): 샘플 입력으로 동작 확인
   ↓
3. [Publish] 클릭
   - 버전 +1
   - 감사 로그 자동 기록
   ↓
4. 실제 운영 적용
   - 이 버전의 자산이 API 호출에 사용됨
```

### 4.2 자산 롤백 절차

```
1. 문제 발견 (운영팀 또는 모니터링)
   ↓
2. 관리자가 Assets Admin → [Rollback]
   - 이전 Published 버전 선택
   - 롤백 사유 입력
   ↓
3. 확인 모달 확인
   ↓
4. 이전 버전이 즉시 적용
   - 감사 로그 기록
   - 관련팀에 알림 전송
```

### 4.3 성능 모니터링 절차

```
매일:
  - Observability Dashboard 확인
  - Error Rate < 1% 확인
  - Response Time p95 < 500ms 확인
  - Token Usage < 80% 확인

주간:
  - 느린 쿼리 상위 5개 검토
  - 에러 패턴 분석
  - 성능 트렌드 (증가/감소)

월간:
  - 용량 계획 (Database, Cache, API)
  - 성능 리포트 생성
  - 임계치 조정 (필요시)
```

### 4.4 설정 변경 절차

```
1. Settings 화면에서 해당 그룹 선택
   ↓
2. 설정값 수정
   - 유효성 검사 자동 수행
   ↓
3. [Save Changes] 클릭
   - 변경 이력 기록
   - 필요시 재시작 알림
   ↓
4. 재시작 필요 시
   - 무중단 재시작 권장
   - 또는 다음 정기 배포시 적용
```

---

## 5. 체크리스트

### 배포 전 (Go-Live Checklist)

- [ ] Assets Admin 테스트
  - [ ] Draft 생성, 수정, Publish 작동 확인
  - [ ] Rollback 작동 확인
  - [ ] 삭제 권한 검증 (draft만 가능)

- [ ] Observability Dashboard 테스트
  - [ ] 모든 메트릭 표시 확인
  - [ ] 시간 범위 필터 작동
  - [ ] 차트 업데이트 (자동 갱신)

- [ ] Settings 테스트
  - [ ] 설정 변경 및 저장
  - [ ] 재시작 요구 플래그 표시
  - [ ] 변경 이력 기록 확인

### 주간 운영 체크리스트

- [ ] Observability 확인 (에러율, 응답시간)
- [ ] Assets 변경 이력 검토
- [ ] Settings 변경 로그 검토
- [ ] 알림/에러 메시지 정리

---

## 6. FAQ

**Q: Assets를 실수로 Publish했는데, 되돌릴 수 있나요?**
A: 네. Rollback을 사용하여 이전 Published 버전으로 되돌릴 수 있습니다. Rollback 버튼을 클릭하고 대상 버전을 선택하세요.

**Q: 왜 일부 자산은 Delete 버튼이 보이지 않나요?**
A: Published 자산은 삭제할 수 없습니다. Draft로 되돌린 후 삭제하세요. (또는 Rollback으로 다른 버전 사용)

**Q: 모니터링 대시보드의 메트릭이 실시간인가요?**
A: 대부분 1-5분 지연으로 업데이트됩니다. [Refresh ⟲] 버튼으로 수동 갱신 가능합니다.

**Q: 설정을 변경하면 즉시 반영되나요?**
A: 대부분은 즉시 반영됩니다. 재시작이 필요한 경우 ⚠️ 표시가 나타나며, 안내에 따라 재시작하세요.

---

**최종 업데이트**: 2026-01-18
**작성자**: Admin UI Team
**검수**: DevOps, Operations
