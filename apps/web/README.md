# Tobit SPA AI

This repository contains a Next.js frontend (apps/web) and a FastAPI backend (apps/api) to support the Tobit SPA AI project.

## How to run locally

- Install dependencies: npm ci
- Run backend: make api-dev
- Run frontend: make web-dev
