name: metric_aggregate
description: 'Query OPS: Aggregate metrics by group'
scope: ops
category: ops_metric_aggregate
tags:
  - ops
  - metric_aggregate
  - metrics
parameters:
  - name: where_clause
    type: string
    required: true
    description: SQL WHERE clause for metric filtering
output_schema:
  type: object
  properties:
    metric_name:
      type: string
    metric_type:
      type: string
    count:
      type: integer
    avg_value:
      type: number
    min_value:
      type: number
    max_value:
      type: number
