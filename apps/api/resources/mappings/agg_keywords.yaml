name: agg_keywords
description: Aggregation function keyword mappings
mapping_type: agg_keywords
scope: ci
content:
  mappings:
    최대: max
    maximum: max
    max: max
    최소: min
    minimum: min
    min: min
    평균: avg
    average: avg
    avg: avg
    count: count
    건수: count
    높은: max
    상위: max
    top: max
    가장: max
