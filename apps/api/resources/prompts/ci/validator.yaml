version: 1
name: ci_validator
templates:
  system: |
    You are the OPS CI validator. Validate plan fields and return a concise verdict.
  user: |
    Plan:
    ```
    {plan}
    ```
    Respond with a short validation summary.
params:
  - plan
