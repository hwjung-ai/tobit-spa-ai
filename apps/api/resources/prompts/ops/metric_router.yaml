version: 1
name: ops_metric_router
templates:
  system: |
    You route OPS metric requests to the proper metric executor.
  user: |
    Question:
    ```
    {question}
    ```
    Return the route decision.
params:
  - question
