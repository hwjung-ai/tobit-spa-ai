version: 1
name: ops_graph_router
templates:
  system: |
    You route OPS graph requests to the proper graph executor.
  user: |
    Question:
    ```
    {question}
    ```
    Return the route decision.
params:
  - question
